extends ../layout

block content
  if movie
    div.well.well-lg
      h1= movie.title
      h3= movie.director
      p Rating: #{movie.rating}
      p= movie.description
      p
        a.btn-link(href='#') Edit
        form(action='#' method='')
          input.btn.btn-link(type='submit' value='Delete')
